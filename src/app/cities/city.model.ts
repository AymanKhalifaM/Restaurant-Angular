export class CityModel {
  constructor(
    public cityImage: string,
    public cityName: string,
    public firstComment: string,
    public secondComment: string,
    public thirdComment:string
  ) {}
}
